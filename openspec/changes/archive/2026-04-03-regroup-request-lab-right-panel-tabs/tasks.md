## 1. OpenSpec 更新

- [x] 1.1 为右侧区两组合并 Tab 需求补齐 proposal、design、tasks 和 delta spec
- [x] 1.2 更新现有 `request-lab-right-panel-tabs` 能力的默认页签与自动跳转描述

## 2. 前端调整

- [x] 2.1 将右侧区顶层 Tabs 从四项调整为两项分组
- [x] 2.2 将 `Python 代码` 与 `请求测试结果` 放入第一组，将 `测试摘要` 与 `简化代码` 放入第二组
- [x] 2.3 在字段分析成功后自动切换到第二组

## 3. 验证与归档

- [x] 3.1 重新生成 bindings 并运行前端构建验证
- [x] 3.2 同步主 spec 并归档 OpenSpec change
