## 1. OpenSpec

- [x] 1.1 新增“前端共享组件抽取重构”变更提案
- [x] 1.2 记录组件分层、抽取边界与验证标准

## 2. 前端重构

- [x] 2.1 将 `App.jsx` 收敛为应用壳层和工作区切换容器
- [x] 2.2 抽取字段探针工作区组件及相关共享展示组件
- [x] 2.3 抽取资源监听 opener 组件并接回原有业务流程
- [x] 2.4 修正字段探针请求输入卡片的内部滚动布局
- [x] 2.5 禁止页面最外层容器参与垂直滚动与回弹
- [x] 2.6 放宽主工作台容器宽度，占用约 90% 视口
- [x] 2.7 为应用壳层、字段探针页、资源监听页和侧边栏补齐宽高响应式布局

## 3. 验证与归档

- [x] 3.1 运行前端构建或等价打包校验
- [ ] 3.2 完成后更新 OpenSpec 任务状态并准备归档

## Notes

- `npm run build` 仍被仓库当前 worktree 缺失的 `wailsjs` 生成目录阻塞，不是本次组件抽取引入的模块语法问题。
- 已使用 `npx esbuild frontend/src/App.jsx --bundle --format=esm --loader:.jsx=jsx '--external:../wailsjs/*' '--external:../../wailsjs/*' '--external:../../../wailsjs/*'` 完成等价打包校验，确认本次重构后的模块结构可被成功打包。
