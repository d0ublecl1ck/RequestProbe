[project]
name = "requestprobe-resource-monitor"
version = "0.1.0"
description = "Python runtime dependencies for RequestProbe resource monitoring"
requires-python = ">=3.13,<3.14"
dependencies = [
  "DrissionPage>=4.1,<5",
]

[tool.uv]
package = false
